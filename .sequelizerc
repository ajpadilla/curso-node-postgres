const path = require('path');

module.exports = {
  'config': path.resolve('config', 'config.js'),            // config stays in config folder
  'models-path': path.resolve('database', 'models'),         // models inside database/models
  'seeders-path': path.resolve('database', 'seeders'),       // seeders inside database/seeders
  'migrations-path': path.resolve('database', 'migrations')  // migrations inside database/migrations
};
